# Code Minimap

## Build Infrastructure
- `README.md` — top-level project status, current measured wins vs upstream `libjxl`, threading status, build/benchmark entrypoints, encoder scaffold status, attribution note, and links to upstream-facing optimization notes
- `NOTICE` — non-clean-room derivative-work attribution note describing upstream `libjxl` lineage, mixed copyright ownership, and the BSD-3-Clause + PATENTS licensing surface for this fork
- `.github/workflows/libjxlz_ci.yml` — repo-specific GitHub Actions workflow running on pushes to `yolo` and pull requests, with Linux `x86_64` flake/full-suite checks plus native `aarch64` test runs on Linux and macOS; the Linux flake checks also cover `x86_64-windows-gnu` cross-compilation, and this is intentionally the only checked-in GitHub Actions workflow so fork CI reflects `libjxlz`, not upstream `libjxl`
- `build.zig` — Zig build config: core static lib (`lib`), C-FFI static lib (`capi`), `djxlz`, installed encode benchmark executables (`bench_modular_encode_prep`, `bench_modular_encode_codestream`), ReleaseFast default; `zig build test` now runs the core library tests, both benchmark harness tests, and the C-FFI unit tests, and the install artifacts are now attached to Zig's default install step so `zig build install` and the flake package emit real binaries/libs
- `build.zig.zon` — Package manifest
- `flake.nix` — Nix dev shell, Garnix CI checks, and package definitions; the package side now exposes both `default` and `debug` install trees by copying Zig's `zig-out` into `$out`, and Linux `x86_64` flake checks still include the dedicated `windows-x86_64-cross` derivation that reuses the checked-in smoke script so Garnix catches Windows portability regressions too
- `build` — Bash: routes native local builds through `nix build` (`packages.<system>.default` or `.debug`) instead of host `zig build`, then repoints `zig-out` at the resulting store path for downstream scripts
- `test` — Bash: runs the flake `checks.<system>.test` derivation for Zig unit/integration coverage, then runs each CLI smoke under `nix develop`, and accumulates errors
- `bm` — Bash: self-enters the dev shell, builds the flake package output once, compiles the public-API decode benchmark harness against the packaged `libjxlz_capi.a`, reuses the packaged `bench_modular_encode_codestream` binary, runs `hyperfine` on the decode scenarios plus the working `600x300` multi-group RGB encode path, appends results to the source-controlled history TSVs, and fails loudly on >5% shifts from the previous logged run
- `include/jxl/jxl_export.h` / `include/jxl/version.h` — generated-support compatibility headers so the upstream public `jxl/*.h` headers in `lib/include` can be used directly by external C consumers and tests
- `bench_decode_runtime.zig` — Runtime decode benchmark harness (`--repeat`, `--reader reference|specialized`, preloaded inputs, full frame decode) with sampled image fingerprints plus known-fixture checksum verification (`--no-verify-known`, `--print-checksum`) so benchmarking catches partial/zero decodes instead of timing them
- `bench_weighted_predict.zig` — Synthetic weighted-predictor microbenchmark (`--repeat`, `--width`, `--height`, `--mode generic_null_props|no_props`) that now compares the generic null-properties callsite against the encoder-style `predictNoProps` path, with stable checksums and parity coverage before predictor-only changes are tried in full-frame decode
- `bench_modular_encode_prep.zig` — Synthetic encoder-side modular prepass harness: generates deterministic image planes, sweeps predictors per pixel, picks the minimum-absolute-residual predictor, tokenizes packed residuals with `HybridUintConfig`, now supports `--bookkeeping instrumented|minimal` so `./bm` can compare the checked/profiler-friendly path with a less instrumented fast path before a real modular writer exists
- `bench_modular_encode_codestream.zig` — Real narrow encoder benchmark harness for the working `600x300` multi-group RGB lossless modular path: assembles DC-global and local group sections, now reusing a flat-histogram info-table cache across repeated group shapes, writes a full codestream, exposes deterministic `--print-checksum` / `--expect-checksum`, and serves as the new primary encode benchmark input for `./bm`
- `tests/cli/capi_decode.c` / `tests/cli/capi_decode.sh` — external C smoke test that compiles against the real upstream `jxl/decode.h`, links the flake-built `libjxlz_capi.a`, and decodes the 4x4 fixture through the public `JxlDecoder` flow
- `tests/benchmark/decode_public_api.c` — checked-in in-memory public-API benchmark harness in C; loads inputs once, reuses `JxlDecoder` + output storage across repeats, decodes through `JxlDecoder` only, and hashes RGB output for deterministic correctness checks
- `tests/benchmark/public_api_decode_history.tsv` — source-controlled benchmark history for the public-API harness (`./bm`)
- `tests/benchmark/modular_encode_prep_history.tsv` — source-controlled timing history for the synthetic encoder prepass benchmark run by `./bm`, now tracking both the fully instrumented and minimal-bookkeeping scenarios
- `tests/benchmark/modular_encode_codestream_history.tsv` — source-controlled timing history for the real narrow `600x300` multi-group RGB encode harness run by `./bm`
- `tests/cli/capi_bench_smoke.sh` — checksum smoke test proving the checked-in public-API benchmark harness compiles against the flake-built `libjxlz_capi.a` and decodes deterministically
- `tests/cli/encode_prep_bench_smoke.sh` — checksum smoke test proving the packaged ReleaseFast encoder prepass harness produces the expected deterministic checksums, while still cross-compiling cleanly for `x86_64-linux`
- `tests/cli/encode_codestream_bench_smoke.sh` — checksum smoke test proving the packaged ReleaseFast narrow encode harness emits the expected deterministic codestream checksum, while still cross-compiling cleanly for `x86_64-linux`
- `tests/cli/windows_cross_compile_smoke.sh` — compile-only portability smoke test for `x86_64-windows-gnu`; cross-builds the pure Zig library, C API, `djxlz`, and encoder scaffold so `./test`, Linux Actions, and Garnix all catch Windows regressions before runtime support exists

## src/
- `root.zig` — package root for the pure Zig library, re-exporting the core `src/lib` modules plus the C-FFI module for test discovery
- `capi_root.zig` — `libjxl`-shaped decoder compatibility layer exporting the first public C decoder slice (`JxlSignatureCheck`, `JxlDecoder*` create/input/basic-info/output/decode flow) by reusing the existing Zig decode pipeline and converting into caller-owned interleaved buffers; now includes tested `UINT8` fast paths for RGB/gray output and correct 8-bit scaling from higher source bit depths
- `src/cli/djxlz_root.zig` / `src/cli/djxlz.c` — C CLI entrypoint and implementation for `djxlz`, which dogfoods the public `JxlDecoder` API exclusively and writes PPM/PGM/PAM to files or stdio targets

## src/lib/
- `lib/root.zig` — core module root, re-exports all submodules via refAllDecls
- `testdata/` — Committed raw codestream fixtures used by decode integration tests, including a real 3-group grayscale frame (`lossless_600x10_multisection.jxl`) for validated multi-section dispatch/truncation coverage and a real 6-group RGB frame (`lossless_600x300_multigroup_rgb.jxl`) for large multi-group boundary/pixel coverage

### src/lib/base/
- `status.zig` — StatusCode enum, JxlError error set, Status struct for FFI bridging
- `bits.zig` — CLZ, CTZ, floorLog2, ceilLog2 via @clz/@ctz builtins
- `common.zig` — Constants (pi, bits_per_byte), divCeil, roundUp, clamp, safeAdd
- `byte_order.zig` — Endian load/store for 16/32/64-bit ints and floats via std.mem
- `random.zig` — Xorshift128+ PRNG matching C++ jxl::Rng exactly
- `rect.zig` — Rect type for rectangular image regions with intersection/translate/shift
- `float.zig` — IEEE 754 float16 to f32 conversion
- `bit_reader.zig` — 64-bit buffered bitstream reader with deferred refill, bounds checking
- `bit_writer.zig` — first encoder-side writable primitive: LSB-first bit packing with byte-padding semantics matching `BitReader`, now with `ensureUnusedCapacityBits` so hot encode paths can reserve an entire bit burst before repeated `write` calls, proven by direct roundtrip tests and a reserve-without-exposed-bytes regression test
- `pack_signed.zig` — PackSigned/UnpackSigned for zigzag encoding of signed frame offsets

### src/lib/entropy/
- `ans_params.zig` — ANS/prefix coding constants (tab size, max alphabet, signature)
- `ans_common.zig` — AliasTable struct with packed Entry, branchless Lookup, InitAliasTable, GetPopulationCountPrecision, CreateFlatHistogram
- `huffman.zig` — HuffmanCode struct, BuildHuffmanTable (2-level), HuffmanDecodingData with ReadFromBitStream/ReadSymbol, ReadSimpleCode
- `hybrid_uint.zig` — HybridUintConfig: split-exponent scheme for variable-length integers, encode/decode
- `inverse_mtf.zig` — Inverse move-to-front transform (scalar implementation)
- `dec_ans.zig` — ANSCode, ANSSymbolReader (ANS + Huffman + LZ77 + hybrid uint), LZ77Params, ReadHistogram, public decoder-side varlen uint helpers for histogram metadata, DecodeUintConfig, special distance table, retained generic read path plus compile-time-specialized clustered uint readers, decodeHistograms (top-level)
- `enc_ans.zig` — first encoder-side entropy-writing foundations: generic `encodeUintConfig(s)` and `storeVarLenUint8/16`, `SizeWriter`, `Token`, `ANSEncSymbolInfo`, an alias-table-backed ANS info-table builder, `ANSCoder.putSymbol`, minimal one-context degenerate/two-symbol/flat histogram metadata writers plus multi-context flat-histogram bundles for both the all-zero and simple-direct context-map cases, plus new general `writeContextMapFlatHistograms` / `writeContextMapNormalizedHistograms` entry points that delegate the context-map bitstream choice to `enc_context_map.writeContextMap` before writing histogram bodies. The file also contains a new exact non-flat histogram foundation (`normalizeHistogramCounts`, `writeSingleContextNormalizedHistogram`, `writeSimpleContextMapNormalizedHistograms`) that serializes real normalized ANS populations through the decoder’s general-histogram path, and a `ContextualHistogramBundle` builder that now turns a token stream + logical context map into reusable normalized counts, ANS info tables, and a clustered emitted `context_map`. Clustering is no longer limited to exact duplicates: after the earlier identical-table coalescing groundwork, the builder now clusters raw token-value histograms before any emitted `HybridUintConfig` is chosen, which removes the old same-config restriction and allows identical/similar logical contexts to merge even if their incoming configs differ. Seed selection is upstream-shaped (`fastClusterHistogramSeeds`), pairwise merges still use raw histogram population cost, and surviving-seed reassignment now uses an explicit KL-divergence scorer (`histogramKLDivergence`) so coding support/probabilities matter instead of only raw growth delta; the raw population cost still falls back to entropy-only scoring when the unencoded alphabet is wider than the ANS population model can represent directly. The file also exposes the first upstream-shaped adaptive uint-config chooser for emitted histograms: it scores candidate `HybridUintConfig` values against the clustered raw token values, rebuilds encoded token-count histograms under the chosen config, and fills `bundle.uint_configs`, `normalized_counts`, and ANS info tables from that chosen emitted shape instead of blindly preserving caller-supplied configs. Supporting helpers now cover cloning sparse histograms, raw population-cost scoring, rebuilding encoded token counts from raw values, choosing initial seeds, merging raw histogram clusters, KL-based reassignment, and adaptive uint-config choice. It still carries the small upstream-shaped `Histogram` cost model (`add`, `addHistogram`, `alphabetSize`, `maxSymbol`, `shannonEntropy`, `distance`), the general contextual token writer keyed by `context_map[token.context]`, and a minimal single-histogram token-stream writer that packs reversed ANS/extra-bit chunks through a single fixed-capacity chunk slice and pre-reserves the full outgoing bit burst before flushing chunks after real encode profiling identified `writeSingleHistogramTokens` / `BitWriter.write` growth churn as the current hotspot; tests prove metadata roundtrips, exact bit-count agreement, ANS state-update correctness, recovered histogram frequencies via alias tables, duplicate-histogram clustering, cross-config raw-value merges, cost-based near-duplicate merging, distinct-histogram non-merging, fast seed selection, adaptive uint-config choice against a brute-force exact-cost reference, seed reassignment, a KL-divergence regression where the old growth metric chose the wrong coding seed, a non-simple 9-histogram roundtrip, and decoder roundtrip of one- and multi-histogram token streams
- `enc_context_map.zig` — encoder-side multi-context metadata helpers: the original all-zero simple map writer plus direct-entry simple context maps for small histogram counts (`bits_per_entry` 1..3), and now a higher-level `writeContextMap` path that mirrors upstream `EncodeContextMap` closely enough to choose between simple direct entries, raw ANS, and MTF+ANS using exact bit-cost probes through the existing one-context ANS writer; it also adds the forward move-to-front transform needed for encoder-side MTF. Tests cover simple/all-zero roundtrips, MTF invertibility, chooser behavior for “simple beats non-simple” and “non-simple beats simple” cases, plus a direct explicit-MTF roundtrip so the MTF encoder path stays covered even when exact-cost shifts make raw ANS cheaper on a previously MTF-favoring fixture
- `dec_context_map.zig` — DecodeContextMap (simple + ANS-coded non-simple path), VerifyContextMap

### src/lib/codec/
- `field_coders.zig` — U32Distr, U32Enc, U32Coder, U64Coder, F16Coder, BitsCoder read functions plus the first matching write-side helpers (`writeEnum`, `writeExtensions`, `writeAllDefault`), and now a finite-only `F16Coder.write` used by conditional metadata payloads like `spot_color`
- `frame_dimensions.zig` — Block/group constants (kBlockDim=8, kGroupDim=256), FrameDimensions struct with Set, GroupRect, BlockGroupRect, DCGroupRect
- `headers.zig` — SizeHeader (compact image dimensions), PreviewHeader, AnimationHeader readers; aspect ratio table
- `loop_filter.zig` — LoopFilter (Gaborish + EPF parameters) with full readFromBitStream, extension support
- `image_metadata.zig` — BitDepth, ExtraChannel enum, ExtraChannelInfo, ToneMapping, Orientation, ImageMetadata, CodecMetadata, OpsinInverseMatrix, CustomTransformData, plus narrow write-side helpers for the current simple codestream surface (`writeBitDepth`, `writeExtraChannelInfo`, `writeImageMetadata`, `writeCustomTransformData`). The metadata writer now supports the alpha family, the plain shared-field extra-channel family, and both currently practical conditional-payload types: default alpha channels still use `AllDefault`, explicit alpha channels serialize their nested bit depth, dim-shift, name, and `alpha_associated` flag, plain extra channels (`selection_mask`, `depth`, `black`, `thermal`, `optional`) serialize their shared bit depth / `dim_shift` / name fields, `spot_color` serializes its four half-float colorants, and `cfa` serializes `cfa_channel`, while reserved/unknown forms remain rejected. Coverage now includes metadata roundtrips for default alpha, associated alpha, named/subsampled alpha (`dim_shift = 1`), `selection_mask`, subsampled `depth`, `spot_color`, and `cfa`
- `color_encoding.zig` — ColorSpace, WhitePoint, Primaries, TransferFunction, RenderingIntent, Customxy, CustomTransferFunction, ColorEncoding with full readFromBitStream plus narrow non-ICC write-side helpers for the current grayscale/RGB fixtures (`writeCustomTransferFunction`, `writeColorEncoding`)
- `frame_header.zig` — FrameEncoding, ColorTransform, FrameType, BlendMode, BlendingInfo, YCbCrChromaSubsampling, Passes, AnimationFrame, FrameHeader with full readFromBitStream
- `toc.zig` — TOC reading (section sizes + Lehmer-coded permutation via ANS), numTocEntries, acGroupIndex, `computeGroupOffsets` for validated section-layout construction
- `enc_toc.zig` — first encoder-side frame-shell helper, currently just the no-permutation TOC writer with roundtrip coverage through `toc.readToc`; intended as the size/section-layout foundation for minimal frame wrappers
- `enc_frame.zig` — encoder-side frame-shell helpers: exact-bit-tested native `writeFrameHeader` for the current simple modular grayscale/RGB surface, `writeFrame` for native frame assembly, and the older borrowed-header helper retained while broader header coverage is still incomplete
- `enc_codestream.zig` — first encoder-side full codestream wrapper with native `SizeHeader`, `ImageMetadata`, `CustomTransformData`, and `enc_frame.writeFrame` serializers for the current simple modular path; now includes full codestream roundtrips through header parsing and `FrameDecoder.decodeFrame` for grayscale, tiny RGB, narrow `3x2` RGBA images with default and associated alpha metadata, a narrow `4x2` RGB + `2x1` subsampled alpha codestream that exercises `dim_shift = 1` end-to-end, a full-size `selection_mask` codestream, a narrow `4x2` RGB + `2x1` subsampled `depth` codestream, a full-size `spot_color` codestream carrying four half-float colorants in metadata, a full-size `cfa` codestream carrying `cfa_channel`, a real `600x300` multi-group RGB image using local trees, a matching `600x300` roundtrip that carries an encoded global `gradient_fixed_dc` MA tree plus shared histograms through the DC-global section, a second `600x300` roundtrip with a channel-split global tree that exercises two different histogram contexts during decode, exact-histogram multigroup codestream coverage for the current MA-tree global-context families (`NoWP`, `RefsNoWP`, `WPRefs`) using `enc_ma.canonicalizeTree` so emitted tree bytes and later per-pixel tokenization agree on decoder-visible breadth-first leaf-context order, and the first palette/RCT/squeeze encode-side modular-transform codestream regressions: the encoder now applies forward YCoCg RCT to a transformed copy of the RGB source, emits one modular RCT transform in the DC-global group header, and proves the normal decoder undoes it back to exact RGB pixels; it carries an explicit squeeze transform list through a local-tree grayscale codestream, decodes through `metaApply`, and proves `undoTransforms` reconstructs the exact unsqueezed source pixels; it carries grayscale and RGB explicit palette transforms, grayscale and RGB delta-palette transforms (`left` predictor with explicit delta tuples), the first auto-delta palette-construction slice, an all-implicit RGB palette codestream, an RGB codestream that mixes implicit colors with one explicit delta tuple, and now a palette codestream where a very frequent implicit RGB color is promoted into an explicit row to shorten the emitted indexes
- `dec_frame.zig` — FrameDecoder (frame header + validated TOC section layout + exact section slicing + error-propagating dispatch in `decodeFrameWithReaderStrategy`), ModularFrameDecoder (`decodeGlobalInfoWithReaderStrategy`, explicit unsupported-feature rejection, YCbCr chroma-channel sizing, extra-channel `dim_shift` sizing, global tree + per-group decode with pre-reserved scratch channel storage and copy-width/copy-height hoisted out of the row loop in `decodeGroup`), ModularStreamId
- `codestream_test.zig` — Integration test: parses real JXL codestream (SizeHeader + ImageMetadata + FrameHeader)
- `decode_test.zig` — End-to-end lossless decode tests for small/single-section and real multi-section codestreams, including exact sampled-pixel coverage for the committed 600x300 RGB multi-group fixture, truncated-frame failure coverage, and reference-vs-specialized parity over the committed corpus

### src/lib/modular/
- `ma_common.zig` — MATreeContext enum (6 contexts), kMaxTreeSize, kNumTreeContexts
- `options.zig` — Predictor enum (14 decoders + 2 encoder-only), pixel_type, PropertyVal, encoder-side `TreeMode` / `TreeKind`, and `ModularOptions` fields shared by decode plus the growing upstream-aligned encoder surface
- `weighted.zig` — Weighted predictor Header (7 coefficients + 4 weights) reading, State (predict + updateErrors with division-free approximation, fixed-4 loops forced to `inline for` after hotspot profiling, plus tested `predictNoProps` / `predictNoWPProp` specializations so decode can skip dead WP-property work)
- `dec_ma.zig` — PropertyDecisionNode, Tree, DecodeTree (reads own ANS histograms, decodes nodes, validates height/ranges)
- `enc_ma.zig` — encoder-side MA-tree writer now widened from the old single-leaf special case to a generic tree writer: it transliterates upstream `TokenizeTree` breadth-first token order into Zig, rebuilds the decoder-visible node layout while tokenizing, exposes `canonicalizeTree` so higher-level encoder paths can normalize arbitrary source trees into the decoder’s implicit breadth-first leaf-context order before building exact histogram bundles, writes the current shared-histogram/context-map tree bitstream shape, and keeps `writeSingleLeafTree` as a thin wrapper; tests now cover both single-leaf and split-tree roundtrips through `dec_ma.decodeTree` plus explicit leaf-context canonicalization
- `context_predict.zig` — ClampedGradient, Select, PredictOne (14 predictors), FlatDecisionNode, MATreeLookup, FilterTree (static property elimination + tree flattening), `PropertyUsePlan` (tracks exactly which dynamic properties survive filtering so decode can skip unused per-pixel materialization)
- `transform.zig` — TransformId (RCT/Palette/Squeeze), SqueezeParams, Transform reading; inverse transform machinery (`InvRCT`, `InvHSqueeze`, `InvVSqueeze`, `InvPalette`, `SmoothTendency`) plus the first encode-side forward transform helpers `fwdRCT`, `fwdPalette`, `fwdPaletteWithDeltas`, `fwdPaletteAutoDeltas`, `fwdHSqueeze`, `fwdVSqueeze`, and `fwdSqueeze`, with exact YCoCg, explicit grayscale palette, explicit RGB palette, grayscale delta-palette, RGB delta-palette, auto-delta RGB palette, implicit RGB palette, mixed implicit+delta RGB palette, frequent-implicit RGB palette, luma-ordered explicit RGB palettes, luma-ordered explicit fallback rows in RGB delta palettes, bucketed grayscale/RGB auto-delta selection, and squeeze roundtrip coverage against the existing inverse paths. `fwdPalette` now scans the decoder’s built-in implicit cube palette, keeps infrequent implicit colors out of the explicit palette entirely, promotes very frequent implicit RGB colors back into explicit rows once they cross the narrow upstream-shaped frequency threshold, and sorts plain explicit RGB palette rows by luma instead of raw image order; `fwdPaletteWithDeltas` now keeps exact delta matches first, then reuses those same implicit colors before falling back to explicit rows, and applies that same luma-ordering rule to the surviving explicit RGB fallback rows; `fwdPaletteAutoDeltas` no longer ranks only by exact residual tuple counts and first-seen order, but first buckets nearby residual tuples by a bitdepth-scaled rounding step, then chooses among equally dense buckets by rounded-delta magnitude before emitting the strongest exact tuple from the winning bucket; and `invPalette` uses the full palette width rather than only the explicit-color count, which is required for delta-prefixed palette streams to decode correctly. The file still owns `MetaSqueeze`, `MetaPalette`, `DefaultSqueezeParameters`, `undoTransforms`, and `metaApply`, and the squeeze/palette helpers are now exercised by real full-codestream grayscale and RGB roundtrips via the encoder-side modular header writers
- `modular_image.zig` — Channel (2D pixel storage with row/shrink access), Image (multi-channel container with transforms)
- `enc_encoding.zig` — first modular encoder tokenization/writing slice: converts single-node non-weighted predictor channels into packed residual `Token`s, feeds them into the encoder-side ANS writer, can emit the smallest real global-tree group plus fully local-tree groups for grayscale, small RGB, and cropped/group-tile RGB images using flat one-context channel histograms plus bounded `HybridUintConfig` fallback when raw residual tokens exceed 8-bit direct alphabets; now also includes upstream-transliterated `makeFixedTree` / `predefinedTree` helpers for fixed modular tree families (`trivial`, `ac_meta`, `wp_fixed_dc`, `gradient_fixed_dc`), DC-global writers for encoded global trees plus either shared, flat multi-histogram, or exact normalized-histogram bundles, and the first narrow modular-transform header writers (`writeEmptyModularGroupWithRCT`, `writeEmptyModularGroupWithSqueeze`, `writeEmptyModularGroupWithPalette`, and `writeGlobalTreeDcSectionWithNormalizedHistogramsAndRCT`) so the encoder can carry one YCoCg RCT transform through the DC-global header, an explicit squeeze transform list through a local grayscale modular group, or explicit/implicit grayscale and RGB palette transforms through the local header while still using the existing tree/histogram machinery, including the mixed implicit+delta palette cases where `nb_colors == 0` but explicit delta rows are still present. The file also owns reusable `GroupRect` + exact-bundle builders for the four current MA-tree quadrants (`NoWP`, `WPNoRefs`, `RefsNoWP`, `WPRefs`), multichannel global-tree rect writers for both uniform-context and per-channel-context payloads, the first upstream-shaped no-WP/no-reference per-pixel MA-tree tokenization path (`tokenizeGlobalTreeChannelRectNoWP`, `writeGlobalTreeGroupImageRectNoWP`) with exact token assertions plus direct `modularDecode` roundtrip coverage, a default-weighted no-reference companion path (`tokenizeGlobalTreeChannelRectWPNoRefs`, `writeGlobalTreeGroupImageRectWPNoRefs`) proved against an explicit `weighted.State` reference walk and a weighted-leaf decode roundtrip, a rect-local reference-property path (`tokenizeGlobalTreeChannelRectRefsNoWP`, `writeGlobalTreeGroupImageRectRefsNoWP`) that materializes previous-channel value/difference props just like decode and is covered by both direct `modularDecode` and full-codestream RGB roundtrips, and the combined weighted-plus-reference path (`tokenizeGlobalTreeChannelRectWPRefs`, `writeGlobalTreeGroupImageRectWPRefs`) that exercises the last remaining modular MA-tree quadrant with exact token assertions plus direct and full-codestream roundtrips; the exact-bundle/global-section call sites here now treat histogram bundles as clustered outputs, pass `bundle.context_map` instead of assuming a 1:1 mapping from logical MA-tree contexts to emitted ANS tables, and delegate the context-map bitstream choice to the new `enc_context_map.writeContextMap` path
- `encoding.zig` — GroupHeader reading, ModularDecode/ModularGenericDecompress with retained `ReaderStrategy.reference` path and compile-time-specialized hot-path dispatch for per-channel decoding with WP/reference properties + sparse property/reference materialization driven by `PropertyUsePlan` + narrow no-reference property-mask specializations for the dominant filtered-tree `WP` masks, now with filtered-tree property IDs remapped into tiny local slot spaces and optional inline compact-node MA-tree lookup for the hot no-reference trees + scanline-hoisted top-row slices in the inner pixel loop + `predictNoWPProp` routing when the filtered tree does not consume WP property 15 + MetaApply + transform undo

## docs/plans/
- `doc/upstream_optimization_notes.md` — upstream-facing summary of kept decode optimizations, including which wins are portable back to C/C++, where Zig aided specialization/layout work, and where Zig still has tradeoffs
- `2026-03-06-libjxlz-design.md` — Overall project design document
- `2026-03-06-phase1-foundation.md` — Phase 1 implementation plan
